# Android / Gradle / IDE
*.iml
.idea
.gradle
/local.properties
/.idea/workspace.xml
/.idea/libraries
.DS_Store
.cxx
*.so
build
seekdb-android-inspection/build/
/captures

# Native engine binaries are downloaded at build time or copied locally; never commit them.
